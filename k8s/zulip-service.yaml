apiVersion: v1
kind: Service
metadata:
  name: zulip-service
  namespace: nekazari
  labels:
    app: zulip
    component: server
spec:
  selector:
    app: zulip
    component: server
  ports:
    - port: 80
      targetPort: 80
      name: http
